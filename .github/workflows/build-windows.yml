name: Build Windows Installer

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  waiting:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Source update in progress; use workflow_dispatch only."
